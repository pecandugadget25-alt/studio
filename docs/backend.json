
{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Informasi profil pengguna ETHNO-ARITH dengan fitur gamifikasi.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "nama": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "peran": { "type": "string", "enum": ["siswa", "guru", "admin", "peneliti"] },
        "level": { "type": "number", "description": "Level pembelajaran pengguna." },
        "poin": { "type": "number", "description": "Total XP yang dikumpulkan." },
        "completedModules": { "type": "array", "items": { "type": "string" }, "description": "Daftar ID modul yang telah diselesaikan." },
        "completedComics": { "type": "array", "items": { "type": "string" }, "description": "Daftar ID komik yang telah dibaca." },
        "badges": { "type": "array", "items": { "type": "string" }, "description": "Koleksi lencana prestasi siswa." },
        "tanggalDaftar": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "nama", "email", "peran"]
    }
  },
  "auth": {
    "providers": ["email"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Koleksi profil pengguna dengan data gamifikasi."
    }
  }
}
